name: Crafting Filter Check

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/crafting-filter-check.yml

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - run: npm install --no-audit --no-fund
      - run: node --test test/crafting-ui.test.mjs test/crafting-core.test.mjs
      - run: npm run check
